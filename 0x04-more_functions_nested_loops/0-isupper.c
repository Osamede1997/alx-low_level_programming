#include "main.h"
/**
 * _isupper: checks if int is uppercase
 * @c: char to be tested
 * Return: returns 1 for success
 */

int _isupper(int c)
{
if (c >=65  && c >= 90)
{
return (1);
}
else
{
return (0);
}
}
