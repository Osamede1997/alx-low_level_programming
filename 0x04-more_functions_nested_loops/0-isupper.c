#include "main.h"
/**
 * _isupper: check for upper case letter
 * @c: character to be tested
 * Return: returns 1 or 0
 */

int _isupper(int c)
{
if (c >= 65  && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
