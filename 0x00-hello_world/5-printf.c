#include <stdio.h>
/**
 * main- prints with proper grammar, but the outcome is a piece of art
 * main returns a value of 0 for success.
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
