#include <stdio.h>

/**
 * main - program entry
 * @argc: argument count
 * @argv: argument vector
 * return: 0 if success
 */


int main(int argc, char const *argv[])
{
if (argc > 0)
{
printf("%s\n", argv[0]);
}
return (0);
}
