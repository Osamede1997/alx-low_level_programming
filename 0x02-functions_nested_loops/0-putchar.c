#include "main.h"

/**
 * main - entry point
 * Return: 0 if exited properly and non-zero if otherwise
 */

int main(void)
{
int n = 0;
char p[9] = "_putchar";
while (n < 9)
{
_putchar(c[i])
n++;
}
return (0);
}
