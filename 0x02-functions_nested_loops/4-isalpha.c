#include "main.h"

/**
 * _islower - check if alphabet is uppercase
 * int c - letter being tested
 * Return: Always 0.
 */

int _isalpha(int c)
{
if (c >= 65  && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
